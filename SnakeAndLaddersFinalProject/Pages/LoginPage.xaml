<Page x:Class="SnakeAndLaddersFinalProject.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:language="clr-namespace:SnakeAndLaddersFinalProject.Properties.Langs"
      xmlns:util="clr-namespace:SnakeAndLaddersFinalProject.Utilities"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d"
      d:DesignHeight="768" d:DesignWidth="1152"
      Title="LoginPage" util:PageBackground.Key="Auth" Loaded="PageLoaded">

    <Page.Resources>
        <SolidColorBrush x:Key="LoginBlue" Color="#209AD6"/>
        <SolidColorBrush x:Key="LoginBlueDark" Color="#1A85B3"/>
        <SolidColorBrush x:Key="ButtonPressedColor" Color="#194062"/>
        <SolidColorBrush x:Key="ButtonHoverColor" Color="#30A9E6"/>

        <!-- Botón principal -->
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource LoginBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background"
                                        Value="{StaticResource ButtonHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background"
                                        Value="{StaticResource ButtonPressedColor}"/>
                                <Setter TargetName="ButtonBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox redondeado -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFBF7E0"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Background="Transparent"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource LoginBlue}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PasswordBox redondeado -->
        <Style x:Key="RoundedPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="#FFFBF7E0"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Background="Transparent"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource LoginBlue}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botón tipo link -->
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource LoginBlue}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <TextBlock x:Name="LinkText"
                                       Text="{TemplateBinding Content}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="LinkText" Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle “Login / Sign up” -->
        <Style x:Key="NavLinkToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}">
                            <TextBlock x:Name="Txt"
                                       Text="{TemplateBinding Content}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Txt" Property="Foreground" Value="Black"/>
                                <Setter TargetName="Txt" Property="FontWeight" Value="Bold"/>
                                <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                                <Setter TargetName="Txt" Property="Foreground" Value="{StaticResource LoginBlue}"/>
                            </MultiTrigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Txt" Property="Foreground" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavLinkLoginStyle" TargetType="ToggleButton" BasedOn="{StaticResource NavLinkToggleStyle}"/>
        <Style x:Key="NavLinkSignUpStyle" TargetType="ToggleButton" BasedOn="{StaticResource NavLinkToggleStyle}"/>

        <!-- Estados fijos (usar System.Boolean) -->
        <sys:Boolean x:Key="TabChecked_Login">True</sys:Boolean>
        <sys:Boolean x:Key="TabChecked_SignUp">False</sys:Boolean>
    </Page.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="979" Height="450">
        <Image Source="/Pages/loginFrame.png"
               Stretch="Fill"
               Panel.ZIndex="0"
               Margin="318,29,319,37"/>

        <Grid Panel.ZIndex="1" Height="450" VerticalAlignment="Top" Background="Transparent" Margin="0,10,1,0">
            <Button x:Name="btnSettings"
                    Content="{x:Static language:Lang.btnSettingsText}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Style="{x:Null}"
                    Margin="0,30,138,0"/>

            <!-- Login: subrayado fijo; el click solo muestra 'press' y vuelve -->
            <ToggleButton x:Name="btnNavLogin"
                          Content="{x:Static language:Lang.btnLoginText}"
                          Style="{StaticResource NavLinkToggleStyle}"
                          IsChecked="{Binding Source={StaticResource TabChecked_Login}, Mode=OneWay}"
                          Click="NavLogin"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Top"
                          Margin="355,146,0,0"
                          Width="122"
                          FontSize="18"/>

            <!-- Sign up: gris fijo; el click solo muestra 'press' y vuelve -->
            <ToggleButton x:Name="btnSignUp"
                          Content="{x:Static language:Lang.btnSignUpText}"
                          Click="SignUp"
                          TabIndex="5"
                          Style="{StaticResource NavLinkToggleStyle}"
                          IsChecked="{Binding Source={StaticResource TabChecked_SignUp}, Mode=OneWay}"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Top"
                          Margin="541,147,0,0"
                          Width="100"
                          FontSize="18" Foreground="#FF4B4B4B"/>

            <Label x:Name="lblUsername"
                   Content="{x:Static language:Lang.txtUsernameText}"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="407,175,0,0"
                   Height="23" FontFamily="Yu Gothic UI"/>

            <TextBox x:Name="txtUsername"
                     MaxLength="50"
                     TabIndex="0"
                     Style="{StaticResource RoundedTextBoxStyle}"
                     Width="164"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Top"
                     Margin="0,202,0,0"
                     Background="White"/>

            <Label x:Name="lblPassword"
                   Content="{x:Static language:Lang.pwdPasswordText}"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="407,234,0,0"
                   Height="23" FontFamily="Yu Gothic UI"/>

            <PasswordBox x:Name="pwdPassword"
                         MaxLength="64"
                         TabIndex="1"
                         Style="{StaticResource RoundedPasswordBoxStyle}"
                         Width="164"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Top"
                         Margin="0,257,0,0"
                         Background="White"/>

            <Button x:Name="btnLogin"
                    Content="{x:Static language:Lang.btnLoginText}"
                    Click="Login"
                    TabIndex="2"
                    Style="{StaticResource LoginButtonStyle}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Width="164"
                    Margin="0,300,0,0"/>

            <Button x:Name="btnPlayAsGuest"
                    Content="{x:Static language:Lang.btnPlayAsGuestText}"
                    Click="PlayAsGuest"
                    TabIndex="3"
                    Style="{StaticResource LoginButtonStyle}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Width="164"
                    Margin="0,334,0,0"
                    Background="#FF194053"/>

            <Button x:Name="btnForgottenPassword"
                    Content="{x:Static language:Lang.btnForgottenPasswordText}"
                    TabIndex="4"
                    Style="{StaticResource LinkButtonStyle}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,358,0,0"/>
        </Grid>
    </Grid>
</Page>
