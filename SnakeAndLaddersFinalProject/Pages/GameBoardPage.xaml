<Page x:Class="SnakeAndLaddersFinalProject.Pages.GameBoardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignWidth="1280" d:DesignHeight="720"
      Title="GameBoardPage">

    <Page.Resources>
        <!-- Aquí evitamos usar Double como recurso -->
        <Thickness x:Key="CellMargin">2</Thickness>

        <Style x:Key="BoardCellStyle" TargetType="Border">
            <!-- Sí, aquí van literales; si luego quieres, los pasamos a diccionario separado -->
            <Setter Property="Width" Value="64" />
            <Setter Property="Height" Value="64" />
            <Setter Property="Margin" Value="{StaticResource CellMargin}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="Background" Value="#222222" />
        </Style>

        <!-- SIN DataType, así XAML no intenta resolver game:BoardCell -->
        <DataTemplate x:Key="BoardCellTemplate">
            <Border Style="{StaticResource BoardCellStyle}">
                <TextBlock Text="{Binding Index}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="Bold" />
            </Border>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="16">
            <TextBlock Text="Snakes &amp; Ladders"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="Bold" />
            <TextBlock Text="  -  Game Board"
                       Foreground="White"
                       FontSize="16"
                       Margin="8,0,0,0" />
        </StackPanel>

        <!-- Tablero -->
        <Viewbox Grid.Row="1"
                 Margin="16"
                 Stretch="Uniform"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center">
            <ItemsControl ItemsSource="{Binding Cells}"
                          ItemTemplate="{StaticResource BoardCellTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="{Binding Rows}"
                                     Columns="{Binding Columns}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Viewbox>
    </Grid>
</Page>
